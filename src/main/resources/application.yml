server:
  port: 8080
  servlet:
    context-path: /api-v1
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: root
  jpa:    hibernate:      ddl-auto: update
    show-sql: true
  application:    name: student-registration
logging:
  level:
    org.springframework.web: DEBUG